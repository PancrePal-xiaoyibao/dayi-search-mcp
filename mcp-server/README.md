# dayi MCP server skeleton

这是 Dayi MCP 的初始化骨架。

当前状态：
- 已创建 TypeScript 目录结构
- 已注册 `dayi_query` 工具占位
- 后续需要桥接 Python Core

后续要做：
1. 调用 Python CLI
2. 返回 structuredContent
3. 增加 `dayi_query_medical` / `dayi_query_doctor` / `dayi_query_disease` / `dayi_query_symptom`
4. 完成 npm 发布配置
